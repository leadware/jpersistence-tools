<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:tx="http://www.springframework.org/schema/tx"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd
		http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx.xsd">
	
	<!-- Scan des annotations Spring -->
	<context:component-scan base-package="com.bulk.persistence.tools.test"/>
	
    <!-- Chargement du fichier de propriétés -->
    <context:property-placeholder location="classpath:hibernate.properties" />
    
    
	<!-- Source de données H2 -->
	<bean name="H2DataSource" class="org.springframework.jdbc.datasource.TransactionAwareDataSourceProxy">
		<property name="targetDataSource">
			<bean class="org.springframework.jdbc.datasource.SimpleDriverDataSource">
				<property name="driverClass" value="${hibernate.connection.driver_class}" />
				<property name="url" value="${hibernate.connection.url}" />
				<property name="username" value="${hibernate.connection.username}" />
				<property name="password" value="${hibernate.connection.password}" />
			</bean>
		</property>
	</bean>
	
	<!-- Fournisseur Hibernate -->
	<bean name="hibernateVendorAdapter" class="org.springframework.orm.jpa.vendor.HibernateJpaVendorAdapter">
		
		<!-- Log SQL -->
		<property name="showSql" value="${hibernate.show_sql}" />
		
		<!-- Dialect -->
		<property name="databasePlatform" value="${hibernate.dialect}" />
		
		<!-- Création du schema -->
		<property name="generateDdl" value="${hibernate.hbm2ddl.auto}" />
		
	</bean>
	
	<!-- Loader eaver Time -->
	<bean name="loaderWeaverTime" class="org.springframework.instrument.classloading.InstrumentationLoadTimeWeaver" />
	
	<!-- Gestionnaire de Persistence -->
	<bean name="persistenceUnitManager" class="org.springframework.orm.jpa.persistenceunit.DefaultPersistenceUnitManager">
		<property name="defaultDataSource" ref="H2DataSource" />
		<property name="persistenceXmlLocations">
			<list>
				<value>classpath:META-INF/persistence-test.xml</value>
			</list>
		</property>
	</bean>
	
	<!-- EntityManager Factory -->
	<bean name="JPAHibernateEntityManagerFactory" class="org.springframework.orm.jpa.LocalContainerEntityManagerFactoryBean">
		<property name="persistenceUnitManager" ref="persistenceUnitManager" />
		<property name="jpaVendorAdapter" ref="hibernateVendorAdapter" />
		<property name="loadTimeWeaver" ref="loaderWeaverTime" />
		<property name="jpaDialect">
			<bean class="org.springframework.orm.jpa.vendor.HibernateJpaDialect" />
		</property>
	</bean>
	
	<!-- Gestionnaire des Transactions -->
	<bean name="JPAHibernateTransactionManager" class="org.springframework.orm.jpa.JpaTransactionManager">
		<property name="entityManagerFactory" ref="JPAHibernateEntityManagerFactory" />
		<property name="jpaDialect">
			<bean class="org.springframework.orm.jpa.vendor.HibernateJpaDialect" />
		</property>
	</bean>
	
	<!-- Détection des Transaction par annotations -->
	<tx:annotation-driven transaction-manager="JPAHibernateTransactionManager" />
	
	<!-- Traduction des Exceptions -->
	<bean class="org.springframework.dao.annotation.PersistenceExceptionTranslationPostProcessor" />
	
	<!-- Gestionnaire des annotations de Persistance -->
	<bean class="org.springframework.orm.jpa.support.PersistenceAnnotationBeanPostProcessor" />
	
</beans>